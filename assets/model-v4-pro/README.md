# Asset Metadata

## Common metadata

- `name`: `v4-pro`
- `type`: `model`
- `version`: `api-binding-v1`
- `description`: 通过 OpenAI-compatible API 使用 v4-pro 的模型调用资源。

## Resource boundary

本资源只声明模型调用协议及运行时环境变量名称，不保存 API Key。未来切换到 Trisol 时由新的 Model adapter 解析对应模型资源。
